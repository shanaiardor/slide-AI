{
  "name": "slide-ask-ai-extension",
  "version": "0.2.1",
  "private": true,
  "description": "A Chrome extension that lets you select text on a page and ask AI.",
  "scripts": {
    "check": "node --check src/background.js && node --check src/content.js && node --check popup/popup.js"
  }
}
